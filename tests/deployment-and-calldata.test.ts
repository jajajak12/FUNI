import { describe, expect, it } from 'vitest';
import { ROBINHOOD_V3_DEPLOYMENT_SPECS, type VerifiedUniswapV3Deployments } from '@funi/core';
import { buildApproval, buildCollect, buildDecrease, buildIncrease, buildMint, canExecuteV3 } from '@funi/v3';

const registry={factory:ROBINHOOD_V3_DEPLOYMENT_SPECS[0].address,positionManager:ROBINHOOD_V3_DEPLOYMENT_SPECS[4].address,swapRouter:ROBINHOOD_V3_DEPLOYMENT_SPECS[7].address,quoter:ROBINHOOD_V3_DEPLOYMENT_SPECS[3].address,multicall:ROBINHOOD_V3_DEPLOYMENT_SPECS[1].address,permit2:ROBINHOOD_V3_DEPLOYMENT_SPECS[8].address,universalRouter:ROBINHOOD_V3_DEPLOYMENT_SPECS[9].address,sourceUrl:'https://developers.uniswap.org',verifiedAt:'fixture',verificationBlock:1n,records:{},relationships:{}} as unknown as VerifiedUniswapV3Deployments;
const token0='0x0000000000000000000000000000000000000001' as const, token1='0x0000000000000000000000000000000000000002' as const;
describe('verified deployment registry and write calldata',()=>{
 it('contains the immutable official v3 contract set',()=>{expect(Object.isFrozen(ROBINHOOD_V3_DEPLOYMENT_SPECS)).toBe(true);expect(ROBINHOOD_V3_DEPLOYMENT_SPECS).toHaveLength(10);expect(ROBINHOOD_V3_DEPLOYMENT_SPECS.find(x=>x.name==='factory')?.address.toLowerCase()).toBe('0x1f7d7550b1b028f7571e69a784071f0205fd2efa');expect(canExecuteV3({status:'unavailable',reason:'code hash mismatch'},false,true,true).allowed).toBe(false);});
 it('constructs real NPM and ERC-20 calldata selectors without broadcasting',()=>{const mint=buildMint(registry,{token0,token1,fee:3000,tickLower:-120,tickUpper:120,amount0Desired:100n,amount1Desired:200n,amount0Min:99n,amount1Min:198n,recipient:token0,deadline:999n});const approval=buildApproval(token0,registry.positionManager,100n);const inc=buildIncrease(registry,{tokenId:1n,amount0Desired:1n,amount1Desired:2n,amount0Min:0n,amount1Min:0n,deadline:999n});const dec=buildDecrease(registry,{tokenId:1n,liquidity:1n,amount0Min:0n,amount1Min:0n,deadline:999n});const collect=buildCollect(registry,{tokenId:1n,recipient:token0,amount0Max:2n**128n-1n,amount1Max:2n**128n-1n});expect(mint.to).toBe(registry.positionManager);expect(mint.data.slice(0,10)).toBe('0x88316456');expect(approval.data.slice(0,10)).toBe('0x095ea7b3');expect(inc.data.slice(0,10)).toBe('0x219f5d17');expect(dec.data.slice(0,10)).toBe('0x0c49ccbe');expect(collect.data.slice(0,10)).toBe('0xfc6f7865');});
});
